'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _ = require('./..');

/**
 * @param {Object} report
 * @param {string} outputForamtName
 * @returns {undefined}
 */

exports.default = function (report, outputForamtName) {
    var output = void 0;

    if (outputForamtName === 'json') {
        output = JSON.stringify(report, '', 4);
    } else {
        var formatter = (0, _.getFormatter)(outputForamtName);

        output = formatter(report);
    }

    /* eslint-disable no-console */
    console.log(output);
    /* eslint-enable no-console */
};

module.exports = exports['default'];
//# sourceMappingURL=outputReport.js.map
